const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('#main-nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));document.querySelectorAll('[data-package]').forEach(link=>link.addEventListener('click',()=>{document.querySelector('#package-select').value=link.dataset.package}));document.querySelector('#booking-form').addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);const subject=encodeURIComponent(`Project enquiry — ${data.get('package')}`);const body=encodeURIComponent(`Hi Homesick Creative,\n\nName: ${data.get('name')}\nEmail: ${data.get('email')}\nService: ${data.get('service')}\nPackage: ${data.get('package')}\n\nProject details:\n${data.get('details')}`);document.querySelector('.form-status').textContent='Opening your email app…';window.location.href=`mailto:hello@homesickcreative.com?subject=${subject}&body=${body}`});
